# 毕设项目 Roadmap 细化（基于 Rust 的混合存储引擎）

## 1. 项目定位与目标

### 1.1 课题定位
本课题目标是实现一个“内存 + 磁盘”协同的混合存储引擎原型，支持基础 KV 操作、简化 SQL 查询、冷热分层与基础事务能力，并通过实验验证其在不同负载下的性能与工程可行性。

### 1.2 核心目标
1. 工程目标：形成可运行、可测试、可复现实验结果的完整系统。
2. 技术目标：体现 Rust 在内存安全、并发控制和系统编程方面的优势。
3. 学术目标：完成具备对比实验和指标分析的毕业论文。

### 1.3 最终可交付成果
1. 可编译运行的 Rust 项目源码（含模块化结构与文档）。
2. 统一查询接口（KV + SQL 子集）演示程序。
3. 测试与 Benchmark 报告（吞吐、延迟、命中率、恢复能力）。
4. 毕设论文、答辩 PPT 与演示视频。

---

## 2. 总体技术方案

### 2.1 技术栈
- 语言：Rust stable
- 并发与异步：`tokio`（用于预热任务、后台清理）
- 序列化：`serde` + `bincode`（或 `rmp-serde`）
- 错误处理：`thiserror` / `anyhow`
- 日志与观测：`tracing` + `tracing-subscriber`
- 测试：`cargo test` + 集成测试 + 基准测试（`criterion`）

### 2.2 系统模块划分
1. 存储层
- 磁盘引擎：B+ Tree、页管理、WAL
- 内存引擎：Skip List、过期管理

2. 调度层
- 冷热识别（滑动窗口）
- 淘汰策略（LRU/LFU）
- 自动预热（异步回填）

3. 查询层
- Redis 协议子集解析（SET/GET/DEL/EXPIRE）
- SQL 子集解析（SELECT/INSERT/UPDATE/DELETE）
- 路由与一致性处理

4. 事务层（加分）
- MVCC Read View
- 行级锁、死锁检测

5. 工程保障
- 配置管理
- 日志与监控
- 单元测试、集成测试、性能测试

---

## 3. 分月实施计划（6 个月）

## 第 1 个月：基础存储引擎（磁盘）

### 本月目标
完成可落盘、可恢复、可范围查询的磁盘存储核心。

### 任务拆解
1. B+ Tree 核心
- 节点结构定义（叶子/内部节点）
- 插入与查询
- 节点分裂与合并
- 范围扫描接口

2. Page Manager + Buffer Pool
- 固定页大小（建议 4KB 或 8KB）
- 页分配与回收
- 缓存页替换策略（先实现简单 LRU）

3. WAL
- 日志追加写
- 崩溃恢复流程（重放）
- 基础 checkpoint 机制（可简化）

### 里程碑
- M1-D10：单线程 B+ Tree 插入/查询通过单元测试
- M1-D20：页管理与 Buffer Pool 打通
- M1-D30：WAL 重放验证通过，系统可恢复

### 验收标准
- 支持 10 万条 KV 的持久化插入与重启恢复
- 范围查询结果正确
- 基础恢复测试通过（异常退出后数据一致）

---

## 第 2 个月：内存引擎

### 本月目标
完成高性能内存 KV 引擎，并具备 Redis 子集协议能力。

### 任务拆解
1. Skip List 存储
- 插入、查找、删除
- 并发安全封装（`RwLock` 或分段锁）

2. Redis 子集协议
- 支持命令：`SET`/`GET`/`DEL`/`EXPIRE`
- RESP 基础解析与返回编码

3. 生命周期管理
- TTL 存储结构
- 定时/惰性过期清理
- 容量上限与淘汰入口

### 里程碑
- M2-D10：Skip List 功能完整
- M2-D20：Redis 子集协议联调通过
- M2-D30：TTL 与容量控制稳定

### 验收标准
- 基础命令集稳定运行
- 过期键能够在预期时间内清理
- 压测下无明显内存泄漏、无 panic

---

## 第 3 个月：冷热调度器（创新核心）

### 本月目标
建立热度识别与双层迁移机制，实现“热留内存、冷落磁盘”。

### 任务拆解
1. 热度统计
- 滑动窗口计数器
- 访问权重模型（读写可设置不同权重）

2. 淘汰策略
- LRU/LFU 二选一先落地，再做可配置扩展
- 触发条件：容量阈值、访问模式变化

3. 自动预热
- 磁盘命中后异步回填
- 预热节流与重复加载去重

### 里程碑
- M3-D10：热度统计准确可观测
- M3-D20：冷数据驱逐路径打通
- M3-D30：预热机制稳定，命中率提升可测

### 验收标准
- 在读多写少场景下，内存命中率明显提升
- 淘汰与预热不会破坏一致性
- 关键指标可通过日志导出

---

## 第 4 个月：统一查询接口层

### 本月目标
对外提供统一访问入口，支持 KV 与 SQL 子集并自动路由。

### 任务拆解
1. SQL 子集解析
- 语法范围：`SELECT/INSERT/UPDATE/DELETE`
- 条件范围：简单 `WHERE key = ...` 与基础比较

2. 路由执行器
- 根据 key 命中层（内存/磁盘）决定执行路径
- 保证读写流程统一

3. 缓存一致性
- 写穿或写回策略（二选一，建议先写穿）
- 更新两层数据与失效机制

### 里程碑
- M4-D10：SQL parser 基础可用
- M4-D20：路由执行链路打通
- M4-D30：一致性策略通过集成测试

### 验收标准
- KV 与 SQL 子集均可访问同一数据集
- 读写后结果一致，不出现脏读
- 异常路径（解析错误、路由失败）有明确错误返回

---

## 第 5 个月：MVCC 事务（加分项）

### 本月目标
实现可展示的事务能力，支持快照读与并发控制。

### 任务拆解
1. MVCC 版本链
- 事务 ID 与版本可见性规则
- Read View 构建与快照读取

2. 锁机制
- 行级锁（或键级锁）
- 锁等待队列

3. 死锁检测
- wait-for graph 简化实现
- 超时回滚或牺牲者事务回滚

### 里程碑
- M5-D10：Read View 生效
- M5-D20：行级锁与并发写冲突可控
- M5-D30：死锁检测样例通过

### 验收标准
- 可演示不可重复读的规避效果
- 并发事务下结果正确
- 死锁场景可检测和恢复

---

## 第 6 个月：实验验证与论文收尾

### 本月目标
完成实验、论文与答辩材料，保证“可讲、可演示、可复现”。

### 任务拆解
1. 性能实验
- 对比对象：纯磁盘、纯内存、混合引擎
- 负载模型：读多写少、写多读少、读写均衡
- 指标：QPS、P95 延迟、命中率、恢复耗时

2. 方案对比
- 与 Redis + MySQL 双中间件方案对比
- 维度：部署复杂度、开发侵入性、运行指标

3. 论文与答辩
- 论文定稿（背景、设计、实现、实验、总结）
- PPT 与演示脚本
- 答辩问答准备（重点围绕一致性、事务、性能）

### 验收标准
- 图表与结论可复现
- 论文结构完整、逻辑闭环
- 演示流程 10 分钟内稳定完成

---

## 4. 质量保障与项目管理

### 4.1 开发规范
1. 统一代码风格：`cargo fmt` + `clippy`。
2. 分支策略：`main` + `feature/*`。
3. 每周固定里程碑与周报。

### 4.2 测试策略
1. 单元测试：核心数据结构与算法。
2. 集成测试：端到端读写、一致性、恢复流程。
3. 压力测试：多线程并发、热点切换、崩溃恢复。

### 4.3 风险与应对
1. Rust 学习曲线导致进度延期。
- 应对：先最小可用版本，再迭代增强。

2. 事务模块复杂度高。
- 应对：先交付非事务主线，MVCC 作为加分逐步接入。

3. 实验结果不明显。
- 应对：提前定义指标、固定基线、增加多组负载对比。

4. 答辩演示风险。
- 应对：准备离线录屏、固定数据集与一键脚本。

---

## 5. 建议的论文目录映射
1. 绪论（背景、意义、目标）
2. 相关技术（Rust、B+ Tree、Skip List、MVCC）
3. 系统总体设计（架构与模块）
4. 关键模块实现（磁盘引擎、内存引擎、冷热调度、查询层）
5. 事务与一致性设计（可作为加分章节）
6. 实验设计与结果分析
7. 总结与展望

---

## 6. 最终答辩可强调的亮点
1. Rust 实现系统级项目，体现内存安全和并发可靠性。
2. 冷热分层与自动预热具备明确创新点和工程价值。
3. 从实现到实验再到论文形成完整闭环，具备可复现性。
