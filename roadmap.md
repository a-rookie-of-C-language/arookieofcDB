模块拆解与实现计划（6个月）
第1个月 — 基础存储引擎
先把磁盘引擎做出来，这是整个系统的地基：

B+ Tree 核心实现（节点分裂、合并、范围查询）
页管理器（Page Manager）+ Buffer Pool
WAL（Write-Ahead Log）保证崩溃恢复

技术选型：Rust（直接利用你的 Rust 背景，这是跟其他同学拉开差距的关键）
第2个月 — 内存引擎

跳表（Skip List）实现 KV 存储
兼容 Redis 协议的子集（SET/GET/DEL/EXPIRE 足够）
内存容量上限 + 数据过期清理

第3个月 — 冷热调度器（创新点核心）

访问频率统计（滑动窗口计数器）
LRU/LFU 驱逐策略，热数据留内存，冷数据驱逐到磁盘
自动预热机制（查询命中磁盘后异步加载回内存）

第4个月 — 统一查询接口层

简化版 SQL 解析（支持 SELECT/INSERT/UPDATE/DELETE 和简单 WHERE）
查询路由：自动判断走内存引擎还是磁盘引擎
缓存一致性：写操作同时更新两层，避免脏读

第5个月 — MVCC 事务（加分项）

Read View 实现，支持快照读
行级锁 + 死锁检测
这块直接对应你在补的 MySQL 内功，实现完之后面字节能深度展开聊

第6个月 — 实验与论文

性能对比实验：纯磁盘 vs 纯内存 vs 你的混合引擎，在不同读写比下的吞吐量/延迟
与 Redis + MySQL 双中间件方案对比：配置复杂度、代码侵入性
写论文 + 准备答辩