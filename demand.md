# arookieofcDB Demand

## 1. 核心问题
很多业务同时使用 Redis（缓存）和 MySQL（持久化），常见痛点是“双写不一致”：
1. 先写 MySQL 再写 Redis，第二步失败会导致读到旧缓存。
2. 先删缓存再写库，失败或并发下会出现脏读回填。
3. 应用层补偿逻辑复杂、分散、难验证。

本项目的初衷不是复刻 Redis 或 MySQL，而是把这类一致性问题下沉到同一个引擎层内解决。

## 2. 项目定位
arookieofcDB 是一个单机混合存储引擎原型：
1. 持久化层：`WAL + B+Tree`（类 InnoDB 的“可恢复真相源”）。
2. 缓存层：内存 HashTable（类 Redis 的低延迟访问）。
3. 统一命令层：KV + SQL-like 命令映射到同一引擎语义。

## 3. 一句话目标
在同一引擎内同时提供“缓存速度 + 持久化可靠性”，并由引擎底层保证数据一致，不把双写一致性负担留给业务代码。

## 4. 一致性模型（主线）
### 4.1 单一真相源
1. 以 `WAL + B+Tree` 为最终真相源。
2. 内存缓存仅做加速，不是最终真相。

### 4.2 写入时序（必须固定）
1. 接收写命令（set/update/delete）。
2. 先写 WAL（按策略决定 fsync 时机）。
3. 再更新 B+Tree。
4. 最后更新/失效内存缓存。
5. 只有完成上述顺序后，才对外返回成功。

### 4.3 读取时序
1. `get`：先查缓存，miss 时回源 B+Tree，再回填缓存。
2. `select`：默认与 `get` 一致；后续可扩展 `select --disk` 用于审计校验。

### 4.4 崩溃恢复
1. 重启先加载 snapshot，再回放 WAL。
2. 恢复后数据以持久化层为准，缓存重新构建。

## 5. 三引擎角色定义
1. `memory`：基线引擎（性能基线、功能调试、无持久化）。
2. `wal`：持久化基线（验证恢复链路、无缓存）。
3. `hybrid`：主目标引擎（缓存 + 持久化一致性）。

说明：项目主线应聚焦 `hybrid`，`memory/wal` 主要用于对照与验证。

## 6. 当前范围（In Scope）
1. 单机 CLI。
2. 统一命令抽象与自注册机制。
3. 键支持字符串语义并可自动识别整数/浮点编码。
4. 值支持字符串语义和内部 `raw/int/float` 编码。
5. 核心命令：`set/get/del/len/status`。
6. SQL-like 命令：`add/select/update/delete`。
7. 数值命令：`incr/decr/incrby/incrbyfloat`。
8. TTL：`expire/ttl`。
9. WAL、checkpoint、重启恢复。
10. 命令耗时统计 + 引擎统计（reads/writes/cache hit/miss/wal/fsync）。

## 7. 当前非目标（Out of Scope）
1. 分布式一致性、主从复制、分片。
2. 完整 SQL 解析器/优化器。
3. 生产级事务隔离（MVCC/锁管理）。
4. 完整 Redis/MySQL 协议兼容。

## 8. 验收标准（围绕“一致性”）
1. `cargo test` 全通过。
2. 写入后崩溃重启可恢复（WAL 回放正确）。
3. `hybrid` 下缓存 miss 回源后与持久化层一致。
4. 删除/更新后不会读到旧缓存值。
5. `status` 可观测并解释三引擎差异。

## 9. 里程碑
### M1 已有（基础闭环）
1. 三引擎可切换。
2. WAL + snapshot 恢复可用。
3. KV/SQL-like/TTL/数值命令可运行。

### M2 已完成（主线增强）
1. 强化 `hybrid` 一致性行为与可观测性。
2. 补齐崩溃恢复与一致性测试矩阵。

### M3 已完成（先主线后细节）
1. TTL 持久化（重启后 TTL 语义一致）。
2. LRU/容量控制（仅影响缓存层，不破坏真相源）。
3. 可选读路由策略（如 `select --disk`）。

### M4 当前下一步（文档先行，2026-04-08）
1. 补一份端到端验收手册（按第 8 节逐条映射到可执行命令与预期输出）。
2. 固化崩溃恢复演练流程（checkpoint 前后、截断 WAL、重启回放、TTL 过期场景）。
3. 固化 `status` 观测说明（重点解释 `cache hit/miss`、`wal/fsync`、`ttl_*` 与三引擎差异）。
4. 在手册基础上新增一组可复用的回归脚本，作为后续 M5 的发布门禁。

## 10. 决策原则
1. 优先保证一致性闭环，再追求功能广度。
2. 优先保证 `hybrid` 主线正确，再优化 `memory/wal` 细节。
3. 所有新增特性都要回答：是否会破坏“单一真相源 + 固定写入时序”。
